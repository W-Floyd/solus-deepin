name       : deepin-screenshot
version    : 4.1.4
release    : 22
source     :
    - git|https://github.com/linuxdeepin/deepin-screenshot.git : 4.1.4
license    : GPL-3.0
component  : desktop.deepin
summary    : Advanced tool to make and edit screenshots
description: |
    Deepin screenshot is an advanced tool to make screenshots on Linux desktop environment. It has a user-friendly UI which eases cropping and saving of screenshot.
builddeps  :
    - pkgconfig(dtkwidget)
    - pkgconfig(dtkwm)
    - pkgconfig(Qt5X11Extras)
    - pkgconfig(x11)
    - pkgconfig(xcb-util)
    - pkgconfig(xcb)
    - pkgconfig(xext)
    - pkgconfig(xtst)
setup      : |
    %cmake_ninja
build      : |
    %ninja_build
install    : |
    %ninja_install
